package com.destroystokyo.paper.profile;

// Minimal stub used only for tests. MockBukkit provides a PlayerProfileMock that implements
// this as an interface; make it an interface to avoid IncompatibleClassChangeError.
public interface PlayerProfile {
    // marker interface for tests; add methods here only if required by tests.
}
